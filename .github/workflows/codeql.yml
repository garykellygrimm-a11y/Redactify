name: "Security and Quality Code Scanning"

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  schedule:
    - cron: '30 4 * * 4'

jobs:
  analyze-logic:
    name: CodeQL Logic Scan
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      actions: read
      contents: read

    strategy:
      fail-fast: false
      matrix:
        language: [ 'javascript-typescript', 'rust' ]

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Initialize CodeQL
      uses: github/codeql-action/init@v4
      with:
        languages: ${{ matrix.language }}
        queries: security-extended,security-and-quality

    - name: Autobuild Engine
      uses: github/codeql-action/autobuild@v4

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@v4
      with:
        category: "/language:${{matrix.language}}"

  analyze-markup:
    name: Frontend Quality Scan
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      contents: read
      actions: read

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup Node.js Environment
      uses: actions/setup-node@v4
      with:
        node-version: '24'

    - name: Install HTMLHint and Stylelint
      run: |
        npm install -g htmlhint
        npm install -g stylelint@15 stylelint-config-standard

    - name: Scan HTML Layouts
      run: |
        # find HTML files and produce SARIF; create empty SARIF if none found
        HTML_FILES=$(find . -type f -name "*.html")
        if [ -z "$HTML_FILES" ]; then
          echo "No HTML files found, creating empty SARIF"
          echo '{"version":"2.1.0","runs":[]}' > htmlhint-results.sarif
        else
          htmlhint $HTML_FILES --format sarif > htmlhint-results.sarif || true
        fi

    - name: Scan CSS Stylesheets
      run: |
        # ensure stylelint config exists
        echo '{"extends": "stylelint-config-standard"}' > .stylelintrc.json

        # find CSS files robustly and produce SARIF; create empty SARIF if none found
        CSS_FILES=$(find . -type f -name "*.css")
        if [ -z "$CSS_FILES" ]; then
          echo "No CSS files found, creating empty SARIF"
          echo '{"version":"2.1.0","runs":[]}' > stylelint-results.sarif
        else
          stylelint $CSS_FILES --formatter sarif --output stylelint-results.sarif || true
        fi

    - name: Debug SARIF existence
      run: |
        echo "HTML SARIF:"
        ls -l htmlhint-results.sarif || true
        echo "CSS SARIF:"
        ls -l stylelint-results.sarif || true

    - name: Upload HTML Security Findings
      uses: github/codeql-action/upload-sarif@v4
      with:
        sarif_file: htmlhint-results.sarif
        category: htmlhint

    - name: Upload CSS Security Findings
      uses: github/codeql-action/upload-sarif@v4
      with:
        sarif_file: stylelint-results.sarif
        category: stylelint
