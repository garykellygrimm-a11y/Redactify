name: "Security and Quality Code Scanning"

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  schedule:
    - cron: '30 4 * * 4' # Automated scan every Thursday at 4:30 AM UTC

jobs:
  # --- LOGIC LAYER: CodeQL for Rust & TypeScript ---
  analyze-logic:
    name: CodeQL Logic Scan
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      actions: read
      contents: read

    strategy:
      fail-fast: false
      matrix:
        language: [ 'javascript-typescript', 'rust' ]

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Initialize CodeQL
      uses: github/codeql-action/init@v3
      with:
        languages: ${{ matrix.language }}
        queries: security-extended,security-and-quality

    - name: Autobuild Engine
      uses: github/codeql-action/autobuild@v3

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@v3
      with:
        category: "/language:${{matrix.language}}"

  # --- MARKUP LAYER: Linters for HTML & CSS ---
  analyze-markup:
    name: Frontend Quality Scan
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      contents: read

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup Node.js Environment
      uses: actions/setup-node@v4
      with:
        node-version: '20'

    # 1. HTMLHint SARIF scan
    - name: Scan HTML Layouts
      run: |
        npm install -g htmlhint
        htmlhint **/*.html --format sarif > htmlhint-results.sarif || true

    # 2. Stylelint SARIF scan (corrected glob + SARIF-capable version)
    - name: Scan CSS Stylesheets
      run: |
        npm install -g stylelint@15 stylelint-config-standard
        echo '{"extends": "stylelint-config-standard"}' > .stylelintrc.json
        stylelint **/*.css --formatter sarif --output stylelint-results.sarif || true

    # 3. Debug SARIF existence
    - name: Debug SARIF existence
      run: ls -l stylelint-results.sarif || echo "SARIF file missing"

    # 4. Upload HTML SARIF
    - name: Upload HTML Security Findings
      uses: github/codeql-action/upload-sarif@v3
      with:
        sarif_file: htmlhint-results.sarif
        category: htmlhint

    # 5. Upload CSS SARIF
    - name: Upload CSS Security Findings
      uses: github/codeql-action/upload-sarif@v3
      with:
        sarif_file: stylelint-results.sarif
        category: stylelint
